/**
 * Data access for the sales module. All Drizzle queries belong here.
 *
 * TODO: implement once `packages/database` defines the sales schema.
 * See `modules/clients` for the reference structure.
 */
export const salesRepository = {};
