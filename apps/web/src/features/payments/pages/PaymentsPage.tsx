import { ModuleStub } from "@/components/common/ModuleStub.tsx";

export function PaymentsPage() {
  return (
    <ModuleStub
      index={5}
      total={11}
      title="Payments"
      description="Receivables and payables with due dates, partial payments and payment history linked back to invoices."
    />
  );
}

export default PaymentsPage;
